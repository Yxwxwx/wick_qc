find_package(GTest 1.15 CONFIG REQUIRED)
add_executable(wickqc_unit_tests test_wick.cpp test_ndarray.cpp test_backend.cpp test_mp2.cpp)
target_link_libraries(wickqc_unit_tests PRIVATE wickqc_runtime GTest::gtest_main)

function(wickqc_dimension_tests target prefix)
  foreach(shape IN ITEMS small alternate)
    add_test(NAME ${prefix}_${shape} COMMAND ${target})
    if(shape STREQUAL "small")
      set(dimensions "WICKQC_TEST_OCCUPIED=2;WICKQC_TEST_ACTIVE=2;WICKQC_TEST_VIRTUAL=3")
    else()
      set(dimensions "WICKQC_TEST_OCCUPIED=3;WICKQC_TEST_ACTIVE=3;WICKQC_TEST_VIRTUAL=2")
    endif()
    set_tests_properties(${prefix}_${shape} PROPERTIES
      ENVIRONMENT "${dimensions};OMP_NUM_THREADS=2;TBLIS_NUM_THREADS=2;OPENBLAS_NUM_THREADS=2;MKL_NUM_THREADS=2;BLIS_NUM_THREADS=2"
      LABELS "${prefix}")
  endforeach()
endfunction()
wickqc_dimension_tests(wickqc_unit_tests unit)
add_executable(wickqc_lapack_tests test_lapack.cpp)
target_link_libraries(wickqc_lapack_tests PRIVATE wickqc_lapack GTest::gtest_main)
wickqc_dimension_tests(wickqc_lapack_tests lapack)

# Ordinary unit tests do not depend on building method examples. These separate
# integration tests check the kernels actually generated by the CMake example.
if(WICKQC_BUILD_EXAMPLES)
  add_executable(wickqc_codegen_tests test_codegen.cpp)
  target_link_libraries(wickqc_codegen_tests PRIVATE wickqc_runtime wickqc_example_kernels GTest::gtest_main)
  wickqc_dimension_tests(wickqc_codegen_tests codegen)
endif()

# Two ordinary consumers include only the umbrella; no project object library
# is linked. This catches missing inline definitions and ODR/link failures.
add_executable(wickqc_header_only_tests test_header_only.cpp header_only_peer.cpp)
target_link_libraries(wickqc_header_only_tests PRIVATE wickqc::wickqc GTest::gtest_main)
wickqc_dimension_tests(wickqc_header_only_tests header_only)
